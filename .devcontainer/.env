# Node environment
NODE_ENV=development

# App environment variables
# React requires them to be prefixed with REACT_APP_
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_NAME=React Starter
REACT_APP_PORT=3000

# Container settings
# You can change the user inside the container if needed
USER=node

# Optional: timezone for consistent container builds
TZ=UTC

# Optional: configure npm registry if needed
NPM_CONFIG_REGISTRY=https://registry.npmjs.org/

